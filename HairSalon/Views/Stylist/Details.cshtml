@{
  Layout = "_Layout";
}

@model HairSalon.Models.Stylist;


@using HairSalon.Models;

<h1>@Model.Name</h1>
<h4>@Model.Details</h4>
<ul>
    @foreach(Client client in Model.Clients)
    {
        <li>@Html.ActionLink($"{client.Name}", "Details", "Client",new { id = client.ClientId })</li>
    }
</ul>


<p>@Html.RouteLink("Add1 Client", "StylistClient", new {id = Model.StylistId })</p>
<p>@Html.ActionLink("Add Client", "Create", "Client", new {id = Model.StylistId })</p>
<p>@Html.ActionLink("Add Client", "Create", "Client")</p>
<p>@Html.ActionLink("See Clients", "Index", "Client")</p>
